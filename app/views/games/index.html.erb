<h1>Games#index</h1>

<p><%= link_to "Create Game", new_game_path, class: "btn btn-primary" %></p>

<ul>
  <% @games.each do |game| %>
    <li>
      <%= link_to game.title, game_path(game) %>
    </li>
  <% end %>
</ul>
